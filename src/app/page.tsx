import ChatBot from "@/components/home/ChatBot";

const Home = () => {
  return (
    <div className="bg-cookcraft-white h-screen w-screen">
      <ChatBot />
    </div>
  );
};

export default Home;
